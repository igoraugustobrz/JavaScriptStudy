// Exercício
const total = 10 + 5 * 2 / 2 + 20; // 35

// Crie duas expressões que retornem NaN
let frase2 = 'Isto é uma string';
let num2 = 30;
console.log(0 / 'oi'); // NaN
console.log(30 * 'opa'); // NaN

// Somar a string '200' com o número 50 e retornar 250
const string = '200';
const num3 = 50;
console.log(+string + num3);

// Incremente o número 5 e retorne o seu valor incrementado
console.log(++5);

// Divida o peso por 2
const numero = '80';
const unidade =  'kg';
const peso = numero + unidade // '80kg'
const pesoPorDois = (+numero / 2);
console.log(pesoPorDois + unidade);
